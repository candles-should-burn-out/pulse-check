# ADR-0004: Приглашенные участники и агрегированная статистика

Status: In Progress

## Context

Pulse Check должен поддержать общий рабочий контекст, в котором пользователь приглашает других людей, а они используют тот же набор статусов для своих локальных сущностей. При этом privacy boundary из ADR-0001 сохраняется: сервер не становится хранилищем пользовательских сущностей, notes, attributes локальных сущностей и персональных маппингов. Исключение этого ADR - сервер хранит product-level атрибуты пользователя, нужные для узнаваемости людей внутри shared `status_set`.

ADR-0003 вводит server-managed `status_set` и read-only режим для участников. Этот ADR уточняет модель приглашений, дерево участников и агрегированную статистику, которую можно безопасно хранить на backend без раскрытия конкретных локальных данных.

Решение еще не реализовано и фиксирует целевое поведение для будущей разработки.

## Decision

Ввести модель приглашенных участников. Пользователь создает ссылку приглашения или QR-код, передает ее другому человеку, а принявший приглашение пользователь получает `status_set` пригласившего.

Термины:

- "владелец" - пользователь, которому принадлежит `status_set`;
- "пригласивший" - пользователь, создавший приглашение в своем `status_set`;
- "участник" - пользователь, принявший приглашение и использующий `status_set` пригласившего.
- `self_name` - обязательный product-level атрибут пользователя, который сам пользователь указывает для отображения в Pulse Check; хранится на backend в user attributes и видно пользователям, которым доступен этот пользователь как участник.
- `alias_name` - локальное имя, которое пользователь задает для другого доступного ему участника; хранится только на frontend, не передается на backend и используется как основной label в UI.

Ключевые правила:

- продуктовый термин для UI - "участник";
- каждый пользователь может иметь только один `status_set`;
- участник наследует `status_set` пригласившего пользователя;
- участник может просматривать статусы, но не может создавать, редактировать или удалять их;
- участник может приглашать других пользователей в тот же `status_set`;
- ссылка приглашения создается backend-ом как одноразовый token с ограниченным сроком жизни;
- срок жизни ссылки приглашения задается только конфигурацией сервиса при запуске; пользователь не выбирает TTL при создании приглашения;
- QR-код является frontend-представлением ссылки приглашения и не требует отдельной backend-сущности;
- ссылка приглашения передает token во frontend fragment, например `/join#token=...`, чтобы token не попадал в backend path, query string и access logs;
- backend хранит только hash token-а, а plaintext token возвращает только при создании приглашения;
- публичная самостоятельная регистрация не поддерживается; новый пользователь может создать учетную запись только через действительную ссылку приглашения;
- существующий пользователь не может принять приглашение;
- первый вход приглашенного пользователя проходит через Keycloak invite-scoped registration-only flow, где он создает новую учетную запись и задает login и password;
- после успешной аутентификации нового аккаунта frontend вызывает backend для принятия приглашения текущим authenticated user;
- backend создает связь участника с `status_set` пригласившего;
- backend хранит агрегированную статистику по `status_id` и `user_id`;
- владелец видит агрегированную статистику всех участников своего `status_set`;
- участник видит собственную агрегированную статистику и агрегированную статистику своей ветки приглашений;
- список участников возвращается backend-ом с техническими идентификаторами, связями приглашений и `self_name` из user attributes;
- участник должен указать непустое собственное отображаемое имя, которое хранится на backend как user attribute;
- Keycloak profile name не является источником истины для `self_name` в рамках этого ADR; backend хранит product-scoped user attributes, а Keycloak остается источником истины для authentication;
- технический `user_id` и все поля `*_user_id` хранят UUID пользователя из Keycloak `sub`; backend отклоняет authenticated principal, если `sub` не парсится как UUID;
- пользователь может задать локальный `alias_name` для доступного ему участника, чтобы видеть этого участника под удобным для себя именем;
- если `alias_name` задан, frontend показывает `alias_name` основным текстом, а `self_name` участника показывает во всплывающем окошке при наведении;
- если `alias_name` не задан, frontend показывает `self_name` участника;
- владелец и участник с правом на свою ветку могут удалить участника полностью из `status_set`;
- полное удаление участника удаляет его связь, ветку приглашенных им участников и их агрегированную статистику из этого `status_set`;
- владелец и участник с правом на свою ветку могут заменить участника другим существующим участником из того же `status_set`;
- при замене статистика заменяемого участника удаляется, его прямые дочерние участники становятся дочерними участниками заменившего пользователя, а membership заменяемого участника удаляется;
- активные приглашения, созданные заменяемым участником, отзываются при замене;
- replacement user должен быть активным участником того же `status_set`, не должен совпадать с заменяемым участником и не должен находиться в ветке заменяемого участника;
- замена не должна нарушать `PARTICIPANT_TREE_MAX_DEPTH`;
- владелец и участник с правом на свою ветку могут заблокировать участника без удаления его связи, ветки приглашений и агрегированной статистики;
- заблокированный участник остается в списках и статистике, но его учетная запись блокируется в Keycloak и он не может войти в аккаунт;
- backend сохраняет состояние блокировки в membership для аудита и дополнительно отклоняет product API запросы от уже выданных token-ов заблокированного участника;
- удаление или блокировка участника не удаляет его учетную запись в Keycloak.

Источники истины:

- Keycloak является источником истины для учетных записей, login, password, account-level блокировок и password reset;
- backend является источником истины для приглашений, membership graph, participant-level блокировок, product-level user attributes и агрегированных счетчиков;
- локальное хранилище frontend является источником истины для `alias_name` участников;
- локальное хранилище пользователя остается источником истины для конкретных сущностей, notes, attributes локальных сущностей и персональных маппингов.

## User-flow

### New Flow: Создание приглашения

Потребитель: authenticated user.

Пользователь открывает страницу участников, создает приглашение, получает ссылку и при необходимости показывает QR-код. Пользователь не выбирает срок жизни ссылки: backend рассчитывает `expires_at` по `INVITATION_TOKEN_TTL_HOURS`. Frontend отображает plaintext token только в момент создания приглашения и не сохраняет его на backend.

### New Flow: Принятие приглашения

Потребитель: anonymous user.

Пользователь открывает `/join#token=...`. Frontend читает token из URL fragment, сохраняет его в session storage и проверяет приглашение через backend.

Frontend отправляет пользователя в invite-scoped registration-only flow Keycloak. В этом flow пользователь создает новую учетную запись, задавая login и password; вход в существующую учетную запись для принятия приглашения недоступен. После успешного Keycloak flow пользователь возвращается на `/join`, а frontend вызывает `POST /participant-invitations/accept` с invitation token и `self_name`. Если ссылку открывает уже authenticated user, frontend показывает, что существующий пользователь не может принять приглашение, и не вызывает accept API.

### New Flow: Управление участниками

Потребитель: authenticated user.

Владелец видит всех участников своего `status_set`. Участник видит себя и участников из своей ветки приглашений. Пользователь может задать свой `self_name` в user attributes, назначать локальные `alias_name` для доступных ему участников, отзывать созданные им приглашения, блокировать, разблокировать, заменять и полностью удалять доступных ему участников в рамках правил доступа.

### New Flow: Имена участников

Потребитель: authenticated user.

Пользователь задает обязательный непустой `self_name` как собственный user attribute. Этот атрибут хранится на backend и показывается пользователям, которым доступен этот пользователь как участник. Пользователь, который видит участника в своей ветке, может задать локальный `alias_name` для этого участника. `alias_name` хранится только на frontend и не передается на backend. Если `alias_name` задан, UI показывает `alias_name` основным текстом, а `self_name` участника показывает в tooltip при наведении.

### New Flow: Блокировка и полное удаление участника

Потребитель: authenticated user.

Пользователь выбирает участника из доступной ветки. Блокировка сохраняет membership, ветку приглашений и агрегированную статистику, но блокирует учетную запись участника в Keycloak и не дает ему войти в аккаунт. Backend также отклоняет product API запросы от уже выданных token-ов заблокированного участника. Полное удаление удаляет membership выбранного участника, всю его ветку приглашений и агрегированную статистику этой ветки.

### New Flow: Замена участника

Потребитель: authenticated user.

Пользователь выбирает заменяемого участника и replacement user из существующих участников того же `status_set`. Backend атомарно удаляет агрегированную статистику заменяемого участника, переносит его прямых дочерних участников на replacement user, удаляет membership заменяемого участника и отзывает активные приглашения, созданные заменяемым участником. Replacement user сохраняет свою статистику и user attributes. Локальные `alias_name` на frontend пересчитываются по оставшимся `user_id`. Операция запрещена, если replacement user совпадает с заменяемым участником, находится в его ветке или перенос нарушит лимит глубины дерева.

### Exist Flow: Управление статусами

Потребитель: authenticated user.

Для владельца flow управления статусами не меняется. Для участника существующие страницы статусов открываются в read-only режиме: участник использует статусы владельца, но не меняет их определения.

## Configuration

| Параметр                         | Значение по умолчанию | Назначение                                              |
|----------------------------------|-----------------------|---------------------------------------------------------|
| `INVITATION_TOKEN_TTL_HOURS`     | `48`                  | Срок жизни ссылки приглашения в часах                   |
| `PARTICIPANT_TREE_MAX_DEPTH`     | `5`                   | Максимальная глубина дерева приглашений                 |
| `PARTICIPANT_MAX_PER_STATUS_SET` | `100`                 | Максимальное количество участников в одном `status_set` |

## Database

Backend хранит приглашения и дерево участников в PostgreSQL.

Этот ADR фиксирует, как операции над участниками влияют на агрегированную статистику. Отдельная схема хранения счетчиков, API синхронизации и страницы статистики будут спроектированы отдельно.

Все поля `user_id` и `*_user_id` хранят UUID пользователя из Keycloak `sub`. Pulse Check не поддерживает OIDC providers или Keycloak-настройки, где authenticated user `sub` не является UUID; такой token backend отклоняет до product API.

### Таблица `user_attributes`

Хранит product-level атрибуты пользователя, которые нужны Pulse Check независимо от участия в конкретном `status_set`.

| Поле         | Тип           | Constraint | Ограничения                                                             |
|--------------|---------------|------------|-------------------------------------------------------------------------|
| `user_id`    | `UUID`        | not null   | UUID пользователя из Keycloak `sub`; первичный ключ                     |
| `self_name`  | `TEXT`        | not null   | Собственное отображаемое имя пользователя; trimmed; от 1 до 40 символов |
| `created_at` | `TIMESTAMPTZ` | not null   | Дата создания записи, выставляется backend-ом                           |
| `updated_at` | `TIMESTAMPTZ` | not null   | Дата последнего обновления user attributes, выставляется backend-ом     |

Дополнительные заметки:

- `user_attributes.user_id` - ссылается на Keycloak user `sub`, но не дублирует login, email, password или Keycloak profile fields.
- `user_attributes.self_name` - product-scoped имя пользователя для отображения в Pulse Check; Keycloak profile name не является источником истины для этого поля.
- Запись `user_attributes` создается при принятии приглашения и при миграции существующего пользователя в собственный `status_set`.

### Таблица `status_set_memberships`

Хранит связь пользователя с `status_set`.

| Поле                     | Тип           | Constraint | Ограничения                                                                                                                                                                       |
|--------------------------|---------------|------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `user_id`                | `UUID`        | not null   | UUID пользователя из Keycloak `sub`; первичный ключ; один набор статусов на аккаунт                                                                                               |
| `status_set_id`          | `UUID`        | not null   | Внешний ключ на `status_sets.id` с `ON DELETE CASCADE`; индекс `status_set_memberships_status_set_id_idx`; часть уникального индекса `status_set_memberships_status_set_user_idx` |
| `owner_user_id`          | `UUID`        | not null   | UUID владельца набора статусов из Keycloak `sub`                                                                                                                                  |
| `invited_by_user_id`     | `UUID`        | null       | UUID пригласившего пользователя из Keycloak `sub`; `null` для владельца                                                                                                           |
| `accepted_invitation_id` | `UUID`        | null       | Внешний ключ на `participant_invitations.id`; `null` для владельца                                                                                                                |
| `blocked_at`             | `TIMESTAMPTZ` | null       | Дата блокировки участника в этом `status_set`; `null`, если участник активен                                                                                                      |
| `blocked_by_user_id`     | `UUID`        | null       | UUID пользователя, который заблокировал участника; `null`, если участник активен                                                                                                  |
| `created_at`             | `TIMESTAMPTZ` | not null   | Дата создания связи, выставляется backend-ом                                                                                                                                      |

Дополнительные заметки:

- `status_set_memberships.user_id` - если удаленный участник снова входит в продукт без нового приглашения, backend создает для него собственный `status_set` как для нового владельца.
- `status_set_memberships.invited_by_user_id` - формирует дерево приглашений внутри одного `status_set`; значение должно ссылаться на пользователя из того же `status_set`.
- `status_set_memberships.accepted_invitation_id` - нужно для аудита принятия приглашения и диагностики конфликтов.
- `status_set_memberships.blocked_at` - блокировка синхронизируется с Keycloak account disable и запрещает product API для уже выданных token-ов, но не удаляет membership, дерево приглашений и агрегированную статистику.
- `status_set_memberships.blocked_by_user_id` - нужно для аудита; значение должно ссылаться на пользователя, которому было доступно управление участником на момент блокировки.
- `status_set_memberships_status_set_user_idx` нужен для проверок дерева участников и будущих внешних ключей агрегированной статистики.

### Таблица `participant_invitations`

Хранит invitation lifecycle.

| Поле                 | Тип           | Constraint | Ограничения                                                                                                |
|----------------------|---------------|------------|------------------------------------------------------------------------------------------------------------|
| `id`                 | `UUID`        | not null   | Первичный ключ, генерируется backend-ом                                                                    |
| `status_set_id`      | `UUID`        | not null   | Внешний ключ на `status_sets.id` с `ON DELETE CASCADE`; индекс `participant_invitations_status_set_id_idx` |
| `owner_user_id`      | `UUID`        | not null   | UUID владельца набора статусов из Keycloak `sub`                                                           |
| `invited_by_user_id` | `UUID`        | not null   | UUID пользователя, создавшего приглашение                                                                  |
| `token_hash`         | `TEXT`        | not null   | Уникальный hash token-а; plaintext token не хранится                                                       |
| `expires_at`         | `TIMESTAMPTZ` | not null   | Дата истечения срока действия                                                                              |
| `revoked_at`         | `TIMESTAMPTZ` | null       | Дата ручного отзыва приглашения                                                                            |
| `accepted_at`        | `TIMESTAMPTZ` | null       | Дата успешного принятия одноразового приглашения; `null`, если приглашение еще не использовано             |
| `created_at`         | `TIMESTAMPTZ` | not null   | Дата создания записи, выставляется backend-ом                                                              |

Дополнительные заметки:

- `participant_invitations.token_hash` - вычисляется через cryptographic hash от случайного token-а с достаточной entropy.
- `participant_invitations.expires_at` - рассчитывается backend-ом от `created_at` и `INVITATION_TOKEN_TTL_HOURS`; пользовательский TTL не принимается.
- `participant_invitations.revoked_at` - отозванное приглашение нельзя принять, даже если срок действия не истек.
- `participant_invitations.accepted_at` - после успешного принятия приглашение считается использованным и не может быть принято повторно.

### Таблица `participant_replacements`

Хранит audit trail разрушительной операции замены участника.

| Поле                        | Тип           | Constraint | Ограничения                                                                                                 |
|-----------------------------|---------------|------------|-------------------------------------------------------------------------------------------------------------|
| `id`                        | `UUID`        | not null   | Первичный ключ, генерируется backend-ом                                                                     |
| `status_set_id`             | `UUID`        | not null   | Внешний ключ на `status_sets.id` с `ON DELETE CASCADE`; индекс `participant_replacements_status_set_id_idx` |
| `replaced_user_id`          | `UUID`        | not null   | UUID участника, который был заменен и удален из membership graph                                            |
| `replacement_user_id`       | `UUID`        | not null   | UUID существующего участника, который получил дочернюю ветку замененного участника                          |
| `replaced_by_user_id`       | `UUID`        | not null   | UUID пользователя, который выполнил замену                                                                  |
| `reassigned_children_count` | `INTEGER`     | not null   | Количество прямых дочерних участников, перенесенных на replacement user                                     |
| `created_at`                | `TIMESTAMPTZ` | not null   | Дата замены, выставляется backend-ом                                                                        |

Дополнительные заметки:

- `participant_replacements` не содержит имен участников, user attributes, notes, attributes локальных сущностей или конкретных локальных сущностей.
- `replacement_user_id` должен ссылаться на активную membership на момент операции, но запись audit trail сохраняет id даже если replacement user будет удален позже.

## API

Все product API ручки, кроме проверки ссылки приглашения, требуют authenticated user из Bearer JWT, если OIDC включен. Проверка ссылки приглашения не создает учетную запись и нужна только для безопасного перехода в invite-scoped Keycloak flow. Принятие приглашения требует authenticated user, созданного через registration-only flow по этой ссылке; существующие учетные записи не могут принять invitation token. Если membership текущего пользователя заблокирован, backend возвращает `403 Forbidden` и не выполняет product API действие для уже выданного token-а. Новые login attempts заблокированного участника должны отклоняться Keycloak-ом, потому что backend при блокировке отключает его учетную запись.

Параметр пути `{user_id}` и все `*_user_id` поля в JSON передаются строками в UUID-формате.

Ошибка заблокированного участника:

Код ответа: `403 Forbidden`

```json
{
  "error": "participant_blocked"
}
```

### POST: /participant-invitations

Тело запроса: отсутствует

Код ответа: `201 Created`

Ответ:

```json
{
  "id": "7d2a6f2b-3d4e-4f6a-9a8b-111111111111",
  "invite_url": "https://pulse-check.example/join#token=plaintext-token",
  "token": "plaintext-token",
  "expires_at": "2026-05-23T10:00:00Z",
  "accepted_at": null,
  "created_at": "2026-05-16T10:00:00Z"
}
```

Права доступа: доступно владельцу и участнику. Приглашение создается в `status_set` текущего пользователя. Ссылка приглашения всегда одноразовая. `expires_at` рассчитывается backend-ом по `INVITATION_TOKEN_TTL_HOURS`; API не принимает пользовательский срок жизни приглашения.

Ошибки:

Код ответа: `400 Bad Request`

```json
{
  "error": "invalid_invitation"
}
```

Код ответа: `409 Conflict`

```json
{
  "error": "participant_limit_reached"
}
```

### POST: /participant-invitations/validate

Тело запроса:

```json
{
  "token": "plaintext-token"
}
```

Код ответа: `200 OK`

Ответ:

```json
{
  "valid": true,
  "expires_at": "2026-05-23T10:00:00Z"
}
```

Права доступа: доступно анонимному и authenticated user. Ответ не раскрывает `owner_user_id`, `invited_by_user_id`, имена пользователей или статистику.

Ошибки:

Код ответа: `404 Not Found`

```json
{
  "error": "invitation_not_found"
}
```

Код ответа: `410 Gone`

```json
{
  "error": "invitation_expired"
}
```

Код ответа: `410 Gone`

```json
{
  "error": "invitation_already_used"
}
```

### POST: /participant-invitations/accept

Тело запроса:

```json
{
  "token": "plaintext-token",
  "self_name": "Алексей"
}
```

Код ответа: `201 Created`

Ответ:

```json
{
  "status_set_id": "7d2a6f2b-3d4e-4f6a-9a8b-111111111111",
  "owner_user_id": "11111111-1111-4111-8111-111111111111",
  "user_id": "22222222-2222-4222-8222-222222222222",
  "invited_by_user_id": "33333333-3333-4333-8333-333333333333",
  "self_name": "Алексей",
  "role": "participant"
}
```

Права доступа: доступно только authenticated user, чья учетная запись была создана через invite-scoped registration-only Keycloak flow для этой ссылки приглашения. Существующий пользователь не может принять приглашение, даже если его аккаунт еще не имеет `status_set`. Backend не принимает login и password в product API. `self_name` обязателен, не может быть пустым после trim, ограничен 40 символами и сохраняется в `user_attributes`.

Ошибки:

Код ответа: `401 Unauthorized`

```json
{
  "error": "authentication_required"
}
```

Код ответа: `409 Conflict`

```json
{
  "error": "account_already_has_membership"
}
```

Код ответа: `409 Conflict`

```json
{
  "error": "invitation_requires_new_account"
}
```

Код ответа: `400 Bad Request`

```json
{
  "error": "invalid_self_name"
}
```

Код ответа: `410 Gone`

```json
{
  "error": "invitation_expired"
}
```

Код ответа: `410 Gone`

```json
{
  "error": "invitation_already_used"
}
```

### GET: /participant-invitations

Тело запроса: отсутствует

Код ответа: `200 OK`

Ответ:

```json
[
  {
    "id": "7d2a6f2b-3d4e-4f6a-9a8b-111111111111",
    "expires_at": "2026-05-23T10:00:00Z",
    "revoked_at": null,
    "accepted_at": null,
    "created_at": "2026-05-16T10:00:00Z"
  }
]
```

Права доступа: authenticated user получает активные и отозванные приглашения, созданные им в текущем `status_set`. Plaintext token и `invite_url` не возвращаются.

### DELETE: /participant-invitations/{invitation_id}

Тело запроса: отсутствует

Код ответа: `204 No Content`

Ответ: отсутствует

Права доступа: доступно пользователю, который создал приглашение, и владельцу `status_set`.

Ошибки:

Код ответа: `404 Not Found`

```json
{
  "error": "invitation_not_found"
}
```

### GET: /participants

Тело запроса: отсутствует

Код ответа: `200 OK`

Ответ:

```json
[
  {
    "user_id": "22222222-2222-4222-8222-222222222222",
    "owner_user_id": "11111111-1111-4111-8111-111111111111",
    "invited_by_user_id": "33333333-3333-4333-8333-333333333333",
    "role": "participant",
    "self_name": "Алексей",
    "blocked_at": null,
    "blocked_by_user_id": null,
    "created_at": "2026-05-16T10:00:00Z"
  }
]
```

Права доступа: владелец получает всех участников своего `status_set`. Участник получает себя и участников из своей ветки приглашений. Backend возвращает `self_name` из `user_attributes` участника и не принимает `alias_name`. Заблокированные участники остаются в ответе.

### GET: /users/me/attributes

Тело запроса: отсутствует

Код ответа: `200 OK`

Ответ:

```json
{
  "user_id": "22222222-2222-4222-8222-222222222222",
  "self_name": "Алексей",
  "updated_at": "2026-05-16T10:00:00Z"
}
```

Права доступа: authenticated user получает собственные product-level user attributes.

### PUT: /users/me/attributes

Тело запроса:

```json
{
  "self_name": "Алексей"
}
```

Код ответа: `200 OK`

Ответ:

```json
{
  "user_id": "22222222-2222-4222-8222-222222222222",
  "self_name": "Алексей",
  "updated_at": "2026-05-16T10:00:00Z"
}
```

Права доступа: authenticated user может изменить только собственные product-level user attributes. `self_name` обязателен, не может быть пустым после trim, ограничен 40 символами и хранится на backend в `user_attributes`.

Ошибки:

Код ответа: `400 Bad Request`

```json
{
  "error": "invalid_self_name"
}
```

### DELETE: /participants/{user_id}

Тело запроса: отсутствует

Код ответа: `204 No Content`

Ответ: отсутствует

Права доступа: владелец может полностью удалить любого участника, кроме себя. Участник может полностью удалить только участника из своей ветки приглашений. Полное удаление удаляет всю ветку приглашенных пользователей под удаляемым участником и их агрегированную статистику.

Ошибки:

Код ответа: `403 Forbidden`

```json
{
  "error": "participant_delete_forbidden"
}
```

Код ответа: `404 Not Found`

```json
{
  "error": "participant_not_found"
}
```

### POST: /participants/{user_id}/replace

Тело запроса:

```json
{
  "replacement_user_id": "44444444-4444-4444-8444-444444444444"
}
```

Код ответа: `200 OK`

Ответ:

```json
{
  "replaced_user_id": "22222222-2222-4222-8222-222222222222",
  "replacement_user_id": "44444444-4444-4444-8444-444444444444",
  "reassigned_children_count": 3
}
```

Права доступа: владелец может заменить любого участника, кроме себя. Участник может заменить только участника из своей ветки приглашений. Replacement user должен быть доступен текущему пользователю, принадлежать тому же `status_set`, быть активным, отличаться от заменяемого участника и не находиться в ветке заменяемого участника. Замена атомарно удаляет агрегированную статистику заменяемого участника, переносит его прямых дочерних участников на replacement user, удаляет membership заменяемого участника и отзывает активные приглашения, созданные заменяемым участником.

Ошибки:

Код ответа: `400 Bad Request`

```json
{
  "error": "invalid_participant_replacement"
}
```

Код ответа: `403 Forbidden`

```json
{
  "error": "participant_replace_forbidden"
}
```

Код ответа: `404 Not Found`

```json
{
  "error": "participant_not_found"
}
```

Код ответа: `409 Conflict`

```json
{
  "error": "participant_replacement_would_create_cycle"
}
```

Код ответа: `409 Conflict`

```json
{
  "error": "participant_tree_depth_exceeded"
}
```

### POST: /participants/{user_id}/block

Тело запроса: отсутствует

Код ответа: `204 No Content`

Ответ: отсутствует

Права доступа: владелец может заблокировать любого участника, кроме себя. Участник может заблокировать только участника из своей ветки приглашений. Блокировка не удаляет membership, ветку приглашений и агрегированную статистику. Backend сохраняет `blocked_at` и `blocked_by_user_id`, отключает учетную запись участника в Keycloak и отклоняет product API запросы от уже выданных token-ов.

Ошибки:

Код ответа: `403 Forbidden`

```json
{
  "error": "participant_block_forbidden"
}
```

Код ответа: `404 Not Found`

```json
{
  "error": "participant_not_found"
}
```

### POST: /participants/{user_id}/unblock

Тело запроса: отсутствует

Код ответа: `204 No Content`

Ответ: отсутствует

Права доступа: владелец может разблокировать любого участника, кроме себя. Участник может разблокировать только участника из своей ветки приглашений, если текущий пользователь не заблокирован. Backend очищает `blocked_at` и `blocked_by_user_id` и снова включает учетную запись участника в Keycloak.

Ошибки:

Код ответа: `403 Forbidden`

```json
{
  "error": "participant_unblock_forbidden"
}
```

Код ответа: `404 Not Found`

```json
{
  "error": "participant_not_found"
}
```

## Страницы

### /join

Назначение: принять приглашение по ссылке или QR-коду.

Доступ: anonymous user и authenticated user. Anonymous user сначала проверяет приглашение и проходит invite-scoped registration-only flow в Keycloak, где создает новую учетную запись с login/password. После этого пользователь возвращается на страницу принятия приглашения. Authenticated user не может принять приглашение и видит сообщение, что приглашение доступно только для нового аккаунта. Token приглашения читается frontend-ом из URL fragment и не отправляется в backend path или query string.

Основные элементы:

- состояние проверки ссылки приглашения;
- действие создания учетной записи через Keycloak;
- обязательное поле `self_name` как user attribute;
- подтверждение принятия приглашения;
- сообщение об истекшей или отозванной ссылке;
- сообщение о конфликте, если аккаунт уже привязан к другому `status_set`.

Действия:

- открыть ссылку приглашения - frontend читает token из URL fragment, сохраняет его в session storage и вызывает `POST /participant-invitations/validate`;
- создать учетную запись - пользователь проходит invite-scoped registration-only Keycloak flow, задает login/password и возвращается на `/join`;
- принять приглашение - frontend отправляет `POST /participant-invitations/accept` с token и непустым `self_name`;
- перейти в приложение - после успешного принятия пользователь попадает в `/app`.

Состояния:

- загрузка проверки token-а;
- приглашение действительно;
- приглашение истекло, отозвано или уже использовано;
- пользователь не authenticated;
- создание учетной записи через Keycloak;
- приглашение открыто существующим authenticated user;
- аккаунт уже имеет `status_set`;
- `self_name` пустой или невалидный;
- успешное принятие приглашения;
- ошибка принятия приглашения.

### /app/participants

Назначение: управлять приглашениями, именами участников, `alias_name`, заменой, блокировкой и полным удалением участников.

Доступ: authenticated user. Владелец видит всех участников своего `status_set`. Участник видит себя и участников из своей ветки приглашений.

Основные элементы:

- список участников, доступных текущему пользователю;
- `self_name` участника из backend-owned `user_attributes`;
- локальный `alias_name` участника, хранящийся только на frontend и видимый только текущему пользователю;
- tooltip с `self_name`, если основным текстом показан `alias_name`;
- техническое состояние участника без отображения backend-owned персональных данных;
- признак блокировки участника;
- кнопка создания ссылки приглашения;
- QR-код для созданной ссылки;
- список приглашений текущего пользователя;
- действие отзыва приглашения;
- действие изменения собственного `self_name`;
- действие задания, изменения и удаления локального `alias_name`;
- действие замены участника существующим участником;
- действие блокировки и разблокировки участника;
- действие полного удаления участника.

Действия:

- создать приглашение - frontend отправляет `POST /participant-invitations` без параметров срока жизни, показывает ссылку и генерирует QR-код локально;
- скопировать ссылку - frontend копирует `invite_url` в clipboard;
- показать QR-код - frontend кодирует `invite_url` без отдельного backend API;
- отозвать приглашение - frontend отправляет `DELETE /participant-invitations/{invitation_id}`;
- изменить собственное имя - frontend отправляет `PUT /users/me/attributes`;
- задать `alias_name` участнику - frontend сохраняет `alias_name` локально и показывает `alias_name` основным текстом;
- удалить `alias_name` участника - frontend удаляет `alias_name` локально и возвращается к отображению `self_name`;
- заменить участника - frontend отправляет `POST /participants/{user_id}/replace`, после успеха обновляет дерево участников и статистику;
- заблокировать участника - frontend отправляет `POST /participants/{user_id}/block`, после успеха обновляет список и сохраняет отображаемые имена;
- разблокировать участника - frontend отправляет `POST /participants/{user_id}/unblock`, после успеха обновляет список;
- удалить участника полностью - frontend отправляет `DELETE /participants/{user_id}`, после успеха обновляет список и статистику;

Состояния:

- загрузка списка участников и приглашений;
- пустое состояние без участников;
- пустое состояние без приглашений;
- ошибка изменения `self_name`;
- ошибка локального сохранения `alias_name`;
- ошибка локального удаления `alias_name`;
- ошибка создания приглашения;
- ошибка отзыва приглашения;
- ошибка замены участника;
- ошибка блокировки участника;
- ошибка разблокировки участника;
- ошибка полного удаления участника;
- ошибка достижения лимита участников;
- успешное изменение `self_name`;
- успешное локальное изменение `alias_name`;
- успешное локальное удаление `alias_name`;
- успешное создание приглашения;
- успешная замена участника;
- успешная блокировка участника;
- успешная разблокировка участника;
- успешное полное удаление участника;
- read-only отображение backend-owned данных для участников, которыми текущий пользователь не может управлять.

## Ограничения

- Срок жизни ссылки приглашения задается только конфигурацией сервиса `INVITATION_TOKEN_TTL_HOURS`; значение по умолчанию - 48 часов.
- Ссылка приглашения всегда одноразовая.
- QR-код является представлением одноразовой ссылки и не поддерживает многоразовое принятие.
- Максимальная глубина дерева приглашений по умолчанию равна 5.
- Максимальное количество участников в одном `status_set` по умолчанию равно 100.
- Участник не может менять определения статусов.
- Существующий пользователь не может принять приглашение; приглашение принимает только новый пользователь через создание нового аккаунта.
- Участник не может принять приглашение, если его аккаунт уже имеет `status_set`.
- Заблокированный участник не может войти в аккаунт через Keycloak.
- Backend дополнительно отклоняет product API запросы заблокированного участника, если у него остался ранее выданный token.
- Заблокированный участник остается в membership graph и учитывается в лимите участников до полного удаления.
- Блокировка участника отключает его учетную запись в Keycloak, но не удаляет учетную запись и не удаляет его локальные данные на устройстве.
- Полное удаление участника необратимо удаляет его membership, ветку приглашений и агрегированную статистику этой ветки из `status_set`.
- Замена участника необратимо удаляет membership и агрегированную статистику заменяемого участника, но сохраняет его прямую дочернюю ветку через reparenting на replacement user.
- Replacement user не может находиться в ветке заменяемого участника, чтобы не создать цикл в membership graph.
- Replacement user должен быть активным участником того же `status_set`.
- Замена участника не должна приводить к превышению `PARTICIPANT_TREE_MAX_DEPTH`.
- Backend хранит `self_name` как обязательный product-level user attribute; это имя является частью shared participant UX и не должно использоваться как источник identity для authentication.
- `user_attributes.self_name` ограничен 40 символами после trim.
- `alias_name` хранится только локально на frontend и не передается на backend.
- Backend не хранит конкретные сущности, notes, attributes локальных сущностей или персональные маппинги.
- Backend не должен логировать plaintext invitation token.
- Полное удаление участника из `status_set` не удаляет учетную запись в Keycloak и не удаляет его `user_attributes`.
- Создание учетной записи доступно только через invite-scoped registration-only Keycloak flow с действительным token-ом приглашения; публичная self-registration без приглашения и вход существующим аккаунтом для принятия приглашения не поддерживаются.

## Consequences

- Пользователи могут расширять общий рабочий контекст через ссылки и QR-коды.
- Статистика владельца и участников остается сопоставимой, потому что все используют один `status_set`.
- Участники могут строить собственные ветки приглашений без прав на редактирование статусов.
- Backend получает ответственность за invitation lifecycle, membership graph и агрегированные счетчики.
- Backend должен синхронизировать participant-level блокировку с Keycloak account disable и проверять блокировку во всех product API ручках для защиты от ранее выданных token-ов.
- Backend получает ответственность за server-side product-level user attributes, поэтому privacy boundary становится шире, чем в варианте с полностью локальными именами.
- Frontend получает ответственность за QR-код, clipboard UX, tooltip для `self_name` при наличии `alias_name` и отображение статистики в разрезах.
- Frontend получает ответственность за хранение `alias_name` и его очистку после полного удаления или замены участника.
- Frontend должен явно различать блокировку и полное удаление, потому что блокировка обратима и сохраняет статистику, а удаление необратимо чистит ветку.
- Frontend должен явно различать замену и удаление: замена сохраняет дочернюю ветку через reparenting, удаление удаляет всю ветку.
- Keycloak остается ответственным за учетные записи, пароль и account-level блокировки, поэтому Pulse Check не реализует хранение паролей.
- Invite-scoped registration-only flow добавляет обязательную интеграционную работу с Keycloak, но сохраняет единую модель authentication и не требует хранить пароли в backend.
- Privacy boundary сохраняется для конкретных сущностей, notes, attributes локальных сущностей, `alias_name` и персональных маппингов, но `self_name` становится server-managed user attribute.
- Блокировка участника сохраняет агрегированную статистику и историю связи в `status_set`.
- Замена участника теряет статистику заменяемого участника, но позволяет сохранить подчиненную ветку при организационной замене человека.
- Полное удаление участника необратимо удаляет его агрегированную статистику в данном `status_set`, но не затрагивает его локальные данные на устройстве.

## Проработка

- Спроектировать точный Keycloak invite-scoped registration-only flow: extension, theme, required actions или другой механизм, который разрешает создание учетной записи только по token-у приглашения и запрещает принятие приглашения существующим аккаунтом.
- Спроектировать UX подтверждения блокировки и полного удаления участника с отображением разных последствий и количества участников в удаляемой ветке.
- Спроектировать UX подтверждения замены участника: показать потерю статистики заменяемого участника, количество переносимых прямых детей и имя replacement user.
- Спроектировать отдельный flow агрегированной статистики и определить, как frontend синхронизирует агрегаты.
- Спроектировать миграцию для существующих пользователей: создание self-membership и перенос текущих статусов в собственный `status_set`.
